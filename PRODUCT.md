# Product

<!-- impeccable:product-schema 1 -->

## Platform

web

## Users

面向希望用自然语言完成股票研究、选股和交易决策准备的个人投资者与投研用户。用户可以理解投资目标和风险约束，但不应被要求先上传策略包、管理 StrategyVersion，或理解网站内部运行时实现。

## Product Purpose

LLM TradeBot 是一套以主 Agent 为统一操作层的股票决策平台。用户在对话中提出目标，并为当前任务自由搭配 Skill、内置工具、MCP 服务、数据源和专家能力；系统最终应把过程沉淀为可追溯的任务、数据快照、研究成果、候选列表、交易提案和运行记录。

## Positioning

Agent 统一交互，能力按工作区配置、按任务挂载，结果独立保存，风险与执行保持受控。Skill 提供工作方法，MCP 提供外部行动能力，数据源提供事实输入，专家与专家团提供结构化评审。主 Agent 负责任务理解、能力编排与结果解释，不替代确定性计算、风控审批或订单执行。

## Operating Context

核心业务体验由四个共享同一完整金融 Agent 底座的决策工作台组成：

- `/overview`：主 Agent 通用对话；
- `/stock-research`：个股分析上下文；
- `/screening`：选股上下文；
- `/trading`：交易策略配置与模拟运行台。

市场情报作为决策输入位于主 Agent 与个股分析之间；定时任务和专家评审属于协作与自动化层；能力中心与任务运行记录属于控制和治理层，而不是新的投资策略页面。

只有主 Agent 使用通用对话工作台；个股分析、选股和交易均使用结构化任务页。专家评审独立提供单聊和群聊模式，群聊按公共证据、独立观点、分歧提取、交叉质疑和主 Agent 汇总组织。交易页先保存投资目标、市场范围、信号节奏、模拟周期和不可绕过的风险边界，再挂载 Skill、内置工具、MCP 服务、数据源、专家与专家团并预览运行流程。所有页面共享同一能力注册表和 Agent 编排底座，但不强行使用同一种交互。桌面端适合配置与审阅，移动端优先完成任务创建、状态查看和结果阅读。

能力中心先通过 `/capabilities` 解释工作区注册与任务挂载的边界，再提供 Skill、内置工具、MCP 服务、数据源和专家配置五个独立页面；每页均区分平台预置能力与工作区自定义能力。Skill 负责方法，内置工具负责站内可执行函数，MCP 服务负责外部连接，数据源负责事实输入，专家负责 Persona 评审。

治理入口将 `/runs` 定义为“任务与运行”账本，将 `/usage` 定义为“模型用量”。前者只记录 Task、Run、DataSnapshot 与 Artifact 的状态，后者只展示模型调用消耗，两个概念不得混用。

## Capabilities and Constraints

- 主 Agent 页面使用真实对话链路；已安装 Skill 会进入真实消息请求。
- 自定义 Skill、MCP 连接、数据源目录和专家配置由后端工作区注册表持久化；运行前会校验启用状态与可用性，并把本次选择冻结到任务和能力快照中。
- 底层 Agent 保留 独立 Agent 引擎 式会话、记忆、ReAct 工具循环、任务恢复与安全约束；网站只向业务页面发布经过治理的金融 Skill 与必要通用工具，非金融个人助理、文件整理和社交渠道类 Skill 不进入默认目录。
- 内置金融工具与 MCP 服务分开治理：前者展示 Tool Schema、权限和作用域，后者管理 Server 连接。主对话与结构化任务均使用后端工作区白名单；只有已启用的内置工具、已配置的数据源和通过健康检查的 HTTP MCP 才能挂载。
- 内置投资专家共享同一个 Agent Runtime，以版本化 Persona Prompt 区分巴菲特、芒格、段永平、凯西·伍德和张磊等公开投资框架；专家团保存成员组合与评审协议，由各视角独立分析、交叉质疑后交给主 Agent 汇总，且不得冒充相关人物本人意见。
- 专家评审会创建正式 Task、DataSnapshot 和 Run；每位所选专家按独立会话生成结构化意见，主 Agent 再比较证据、假设与分歧并保存 `ExpertOpinion` 和 `ExpertReview` 成果。
- 个股分析、选股和交易不再要求用户选择、上传或发布旧 StrategyVersion；它们使用同一 Agent 编排底座和结构化配置，但分别产生研究报告、候选列表和受风控约束的模拟交易运行。
- 交易页保存后端交易任务并可手动或定时创建 Run，运行结果持久化为 `TradeProposal`、合同级 `RiskAssessment` 和 `PaperTradingRun`。当前不会生成虚构成交、收益或真实订单；账户级风险评估、模拟撮合和实盘执行仍属于独立的受控执行阶段。
- 旧“策略实验室 / 验证实验 / 版本运行”页面退出主信息架构；历史 URL 只保留到新工作台的兼容跳转。
- 结构化任务运行会持久化 Task、能力快照、DataSnapshot、Run 与 Artifact；对话会话仍沿用现有 Agent 会话存储，交易审批和真实订单审计尚未开放。
- 交易工作台当前只允许生成和解释交易提案；风险检查、审批和订单执行必须由独立确定性服务控制。
- 所有未接入的仓位、收益、订单和交易结果不得用示例 KPI 冒充真实数据。

## Evidence on Hand

- 前端：`apps/dsa-web/` 已接入主 Agent、能力中心、结构化任务、专家评审、任务运行账本和定时任务 API。
- Agent Runtime：网站可选择 独立 Agent 引擎 作为底层模型—工具循环，但产品页面统一显示“主 Agent”，运行时细节集中在设置页。
- 后端：工作区 API 已统一 Skill、内置工具、MCP、数据源、专家、Task、Run、DataSnapshot、Artifact 和 Schedule；金融 MCP 以只读/计算权限发布站内工具。

## Product Principles

- 页面共享同一个 Agent 编排底座，不是四套独立 Agent；页面交互由任务决定，不强制全部使用聊天框。
- 策略是 Agent、自然语言目标、Skill、内置工具、MCP 服务、数据源、专家和约束的可保存组合，不是上传代码包的同义词。
- 主 Agent 是统一交互与编排层，不是数据库、计算引擎、风控引擎或券商终端。
- 聊天用于创建和修改正式成果，不替代成果对象本身。
- 数据快照、证据引用、版本、审批和运行记录必须可追溯。
- 新能力延续现有主题、组件和工作台交互语言，不建立第二套视觉系统。
